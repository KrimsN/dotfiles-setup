{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Метод 'pkg' — имя пакета в apt/dnf/yum",
  "description": "Схема для data/packages/methods/pkg.json. Ключ верхнего уровня (кроме $comment) — имя пакета из registry.json.",
  "type": "object",
  "properties": {
    "$schema": { "type": "string" },
    "$comment": { "type": "string" }
  },
  "additionalProperties": {
    "type": "object",
    "required": ["apt", "dnf", "yum"],
    "properties": {
      "apt": { "type": "string", "description": "Имя пакета для Ubuntu/Debian." },
      "dnf": { "type": "string", "description": "Имя пакета для Fedora." },
      "yum": { "type": "string", "description": "Имя пакета для CentOS/RHEL (обычно через EPEL, см. поле prereq в registry.json)." }
    },
    "additionalProperties": false
  }
}
