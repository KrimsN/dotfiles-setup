# dotfiles-setup: конфиг bat.
# Устанавливается модулем modules/cli-tools.sh в ~/.config/bat/config —
# не редактировать исходник на месте установки, правки перезапишутся
# при повторном запуске установки.

--theme="TwoDark"
--style="numbers,changes,header"
--paging=auto
